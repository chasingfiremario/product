import React from 'react';
import { useParams } from 'react-router-dom';
import { useQuery } from '@apollo/client';
import { GET_PRODUCT_BY_ID } from './allProductsQuery.graphql'; // Import your GraphQL query

const ProductDetail = () => {
  // Use the query to fetch and display a single product
};
export default ProductDetail;
