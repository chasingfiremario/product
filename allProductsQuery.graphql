# Query to fetch all products
query {
  products {
    id
    name
    image
  }
}

# Query to fetch details of a specific product
query {
  product(id: "123") {
    id
    name
    image
    description
  }
}
