import React from 'react';
import { useQuery } from '@apollo/client';
import { GET_ALL_PRODUCTS } from './allProductsQuery.graphql'; // Import your GraphQL query

const AllProducts = () => {
  // Use the query and render the component accordingly
};
export default AllProducts;
